# Changesets

Use one changeset per public package change. Packages version independently; breaking changes require a migration guide and an explicit compatibility-matrix update. Prereleases use the `next` channel until the six-component Solid contract is complete.
