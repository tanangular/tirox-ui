# Native SVG rendering target

`@tirox-ui/svg` ในอนาคตจะใช้ native SVG DOM เป็น rendering target และแชร์ Tirox contracts สำหรับ state, tokens, motion และ accessibility โดยไม่แปลง SVG เป็น canvas เป็นหลัก และไม่ลด SVG interactive components ให้เป็น visual-only
