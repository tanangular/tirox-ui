# Semantic overlay layer tokens

overlay stacking ของ Dialog, Popover และ Tooltip จะใช้ semantic z-index/layer tokens จาก `@tirox-ui/preset` แทนการ hardcode ตัวเลขใน component เพื่อให้ consumer ปรับ scale ตาม Design System ได้ และทำให้ stacking policy อ่านได้จาก token contract
