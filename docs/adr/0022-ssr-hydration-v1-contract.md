# SSR and hydration contract from v1

ทุก component ของ Tirox UI ต้องรองรับ SSR และ hydration ตั้งแต่ v1 พร้อม tests สำหรับ server/client consistency, state initialization และ interaction หลัง hydration เพราะ production SolidJS applications ต้องไม่พึ่ง client-only rendering เป็น baseline
