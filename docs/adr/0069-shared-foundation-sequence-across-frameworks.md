# Shared foundation sequence across frameworks

`@tirox-ui/solid` และ `@tirox-ui/svelte` จะใช้ foundation component set และลำดับเดียวกันคือ `Button → Input → Checkbox → Select → Dialog → Tooltip` เพื่อให้ conformance, documentation parity และ compatibility matrix เปรียบเทียบกันได้โดยตรง
